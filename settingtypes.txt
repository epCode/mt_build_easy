SLOWBUILD_ENABLED (Makes the placing of mass blocks slower, more fancy) bool true

BUILD_SYTLE (Build Style) enum build_tool build_tool,build_mode,classic
# Build tool makes you to need to create a build tool to use the functionality of the mod
# Build mode makes the player have to go into build mode to then start building (no tool needed)
# Classic mode allows the player access to functionality all the time if you hold aux1.
